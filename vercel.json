{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "dist/your-angular-project-name"
        }
      }
    ],
    "routes": [
      {
        "src": "/api/(.*)",
        "dest": "db.json"
      }
    ]
  }
  